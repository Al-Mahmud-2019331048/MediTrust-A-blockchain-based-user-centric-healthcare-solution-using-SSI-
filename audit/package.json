{
  "name": "@meditrust/audit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5",
    "dotenv": "^16",
    "jsonwebtoken": "^9"
  },
  "devDependencies": {
    "prisma": "^5",
    "typescript": "^5"
  }
}
